import { authors_mock } from "./authors-mock";
import { featured_post, last_post, posts_mock } from "./posts-mock";

export const mocks = {
  posts: posts_mock,
  authors: authors_mock,
  last_post: last_post,
  featured_posts: featured_post,
}